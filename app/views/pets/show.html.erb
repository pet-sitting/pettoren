
<div class="container">
  <h1><%= @pet.name %></h1>
  <%cl_image_tag @pet.pet_pic.key, height: 300, width: 400, crop: :fill  %>
  <h3><%= User.find(@pet.user_id).name %></h3>
  <p><%= @pet.species %></p>
  <p><%= @pet.description %></p>
</div>
